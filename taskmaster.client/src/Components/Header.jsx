import HomepageTitle from "./HomepageComponents/HomepageTitle";

function Header(){
return(
    <header className="w-full min-h-[10dvh] flex justify-start items-center p-2 bg-red-500">
        <HomepageTitle />
    </header>
)}

export default Header;